{
  "name": "explore-time-capsule-api",
  "version": "1.0.0",
  "description": "Api for explore-time-capsule",
  "main": "index.js",
  "scripts": {
    "start": "node bin/www",
    "lint": "eslint --ext \".js\" --ignore-path ../.gitignore .",
    "lint:fix": "npm run lint -- --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "AGPL-3.0",
  "devDependencies": {
    "eslint": "^7.30.0"
  },
  "dependencies": {
    "bytes": "^3.1.0",
    "dotenv": "^10.0.0",
    "fastify": "^3.18.1",
    "fastify-cors": "^6.0.1",
    "fastify-static": "^4.2.2"
  }
}
